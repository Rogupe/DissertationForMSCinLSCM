# Data — the anonymised industry dataset must NEVER be committed.
# Only synthetic samples or schema documentation may be added, deliberately (git add -f).
# The anonymisation mapping key reverses the pseudonymisation — never commit or share it.
PRIVATE_*
*mapping_key*
/data/
*.csv
*.tsv
*.xlsx
*.xls
*.parquet
*.feather
*.pkl
*.pickle
*.h5
*.hdf5
*.db
*.sqlite

# Model artefacts / experiment outputs
*.pt
*.pth
*.keras
*.ckpt
results/
outputs/

# Windows
Thumbs.db
Desktop.ini

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# R
.Rhistory
.RData
.Rproj.user/

# LaTeX
*.aux
*.log
*.out
*.toc
*.synctex.gz

# Editores
.vscode/
.idea/

# Claude Code (configuración local)
.claude/settings.local.json

# Archivos temporales de Office
~$*
# Test cache
.pytest_cache/

# Claude Code local preview config (machine-specific paths)
.claude/launch.json

# Personal documents that live next to the docs but must never be published
docs/Comprobante*

# SLR (slr/): saved full texts, workbook backups and raw database exports are never committed
slr/fulltext/
slr/**/fulltext/
slr/**/pre_audit_backup.xlsx
slr/**/*.bak
slr/**/scopus_export*.csv
slr/**/savedrecs*.xlsx
slr/**/*_prefilled.xlsx
slr/**/keywords_by_id.json
slr/**/batch*.txt
# the curated SLR deliverables are tracked despite the global data patterns above
!slr/data/*.csv
!slr/workbook/SLR_PRISMA_workbook_screening.xlsx
